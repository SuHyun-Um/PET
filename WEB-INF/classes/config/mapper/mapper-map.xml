<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="map.dao.MapDAO">

    <select id="selectAllMap" resultType="mapVO">
   <![CDATA[SELECT id, title, link, category, telephone, address, roadAddress, mapx, mapy 
        FROM hosInfo
        ORDER BY id DESC]]>
    </select>
   
</mapper>